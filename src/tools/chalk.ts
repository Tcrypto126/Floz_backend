import chalk from "chalk";

const chalkError = chalk.bold.red;
const chalkSuccess = chalk.bold.green;
const chalkWarning = chalk.bold.yellow;
const chalkInfo = chalk.bold.blue;
const chalkDebug = chalk.bold.magenta;
const chalkTrace = chalk.bold.cyan;

export {
  chalkError,
  chalkSuccess,
  chalkWarning,
  chalkInfo,
  chalkDebug,
  chalkTrace,
};
